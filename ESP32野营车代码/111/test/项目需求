项目设定：
一、这是一个基于ESP32-WROOM-32E开发板的项目，应用场景是室外的野营，主要是辅助用户
二、可以温度采集（DHT11），湿度采集（DHT11），有害气体采集（FC-22 SBX），人体感应（HC-SR501，GPIO32；原光照 GZ-A1 代码在固件中注释保留，可日后恢复），四个马达驱动（L298N模块）
三、ESP32核心板以及相关模块作为感知层，处理层和传输层（底层）；华为云iot设备中心是平台层（中间层）；小程序是应用层（上层）。这是一个完整的物联网相关项目
四、底层将数据传输给中间层，中间层保留数据并将数据传输到上层，上层接收数据同时可以下发命令给中间层，中间层得到指令后将命令下发至底层
五、设备上电之后各个环境传感器模块进行环境数据监测（底层），将数据传递给ESP32进行处理，然后ESP32将数据整理后就上传数据至华为云（中间层），华为云实时显示数据，并且将数据上传至微信小程序中让用户看到（上层），这是数据上传的流程
六、用户在微信小程序上看到后决定下发命令，经华为云下发至设备；当前固件下行命令实现为马达控制（motor_set / motor_all），环境类传感器为只读上云，请与物模型、小程序侧约定一致